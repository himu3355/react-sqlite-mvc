# react-sqlite-mvc
This is basic React JS Model View Controller(MVC) structure with Sqlite database.

### start server

```
cd server
npm install
node server.js
```

### start client
```
cd client
npm install
npm start
```